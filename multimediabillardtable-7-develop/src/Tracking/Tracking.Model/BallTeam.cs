namespace Tracking.Model
{
    /// <summary>
    /// Describes whether a ball is solid color (<see cref="Full"/>),
    /// or only half colored (<see cref="Half"/>)
    /// </summary>
    public enum BallTeam
    {
        Full = 0,
        Half = 1
    }
}