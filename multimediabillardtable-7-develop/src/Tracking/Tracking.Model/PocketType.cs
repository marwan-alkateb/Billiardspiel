namespace Tracking.Model
{
    /// <summary>
    /// Defines a pocket location on a billard table
    /// </summary>
    public enum PocketType
    {
        TopLeft,
        TopMid,
        TopRight,

        BottomLeft,
        BottomMid,
        BottomRight
    }
}